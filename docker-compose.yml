version: '3.9'

services:
  # ---------------------------------------------------------------------------
  # Infrastructure
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: openlintel-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-openlintel}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-openlintel_dev}
      POSTGRES_DB: ${POSTGRES_DB:-openlintel}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U openlintel']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: openlintel-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: openlintel-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data

  # ---------------------------------------------------------------------------
  # Search
  # ---------------------------------------------------------------------------
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
    container_name: openlintel-elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - 'ES_JAVA_OPTS=-Xms512m -Xmx512m'
    ports:
      - '9200:9200'
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data

  # ---------------------------------------------------------------------------
  # Application Services (uncomment as you build them)
  # ---------------------------------------------------------------------------

  # web:
  #   build:
  #     context: .
  #     dockerfile: infra/docker/web.Dockerfile
  #   container_name: openlintel-web
  #   restart: unless-stopped
  #   ports:
  #     - '3000:3000'
  #   environment:
  #     - DATABASE_URL=postgresql://openlintel:openlintel_dev@postgres:5432/openlintel
  #     - REDIS_URL=redis://redis:6379
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy

  # design-engine:
  #   build:
  #     context: .
  #     dockerfile: infra/docker/design-engine.Dockerfile
  #   container_name: openlintel-design-engine
  #   restart: unless-stopped
  #   ports:
  #     - '8000:8000'
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

volumes:
  postgres_data:
  redis_data:
  minio_data:
  elasticsearch_data:
