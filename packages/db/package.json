{
  "name": "@interiorai/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate": "echo 'TODO: run database migrations'",
    "seed": "echo 'TODO: seed database'",
    "reset": "echo 'TODO: reset database'"
  },
  "dependencies": {
    "@interiorai/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
